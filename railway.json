{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": "npx prisma migrate deploy && npx tsx prisma/create-demo-users.ts",
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
