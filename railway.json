{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": "npx prisma migrate deploy && (npx tsx prisma/create-admin.ts || echo 'admin bootstrap skipped (set ADMIN_PASSWORD to enable)')",
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
